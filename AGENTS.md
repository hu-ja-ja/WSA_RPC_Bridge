# Agents.md

## VCS

- VCSはJujutsu(jj-vcs)を使用する。
- 指示されない限りcommitはしない。

## Build

- Androidのbuildはスタックするため、ユーザーが直接実行する。
- gradlew.batは処理がスタックするので、ユーザーが直接実行する。

## Code

- コードに固有名詞は使用しない。
- コメントに固有名詞は使用しない。
